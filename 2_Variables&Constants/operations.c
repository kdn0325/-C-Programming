#include <stdio.h>

int main(void)
{
    int x = 10;
    int y = 20;
    printf("x = %d.",x);
    printf("y = %d.",y);
    printf("x+y = %d.",x+y);
    printf("x-y = %d.",x-y);
    printf("x*y = %d.",x*y);
    // int형으므로 정수만 출력함!
    printf("x/y = %d.",x/y);
    return 0;
}